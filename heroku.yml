build:
  docker:
    web: Dockerfile

# Specify services
services:
  - postgresql

# Specify addons (for additional services like databases)
addons:
  postgresql: "12"

run:
  web: gunicorn bookstore.wsgi:application --bind 0.0.0.0:$PORT